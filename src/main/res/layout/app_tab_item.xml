<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/app_tab_item_layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackgroundBorderless">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="center"
        android:gravity="center"
        android:paddingBottom="6dip"
        android:paddingLeft="8dip"
        android:paddingRight="8dip"
        android:paddingTop="5dip">

        <ImageView
            android:id="@+id/app_tab_item_icon"
            android:layout_width="22dip"
            android:layout_height="22dip"
            android:scaleType="centerInside"/>

        <TextView
            android:id="@+id/app_tab_item_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"/>
    </LinearLayout>

    <cn.com.rooten.ctrl.tab.AppTabTextView
        android:id="@+id/app_tab_item_pop"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:layout_gravity="center_horizontal"
        android:background="@drawable/pop_text_normal_bg"
        android:layout_marginLeft="8dip"
        android:textColor="@color/white"
        android:gravity="center"
        android:textSize="12sp"
        android:visibility="gone"/>
</FrameLayout>